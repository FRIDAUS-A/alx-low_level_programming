#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer
 *
 * Return: Allow success
*/
int _abs(int n)
{
	a = -1 * n;
	if (n > 0)
		return (n);
	else
		return (a);
}
